<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="ticket_close_mail_composer_form" model="ir.ui.view">
        <field name="name">ticket.close.mail.composer.form</field>
        <field name="model">ticket.close.mail.composer</field>
        <field name="arch" type="xml">
            <form string="Compose Email">
                <group>
                    <field name="subject" required="1"/>
                    <field name="body" widget="html" options="{'style-inline': true}"/>
                    <field name="attachment_ids" widget="many2many_binary"/>
                    <field name="template_id" invisible="1"/>
                    <field name="partner_ids" widget="many2many_tags" invisible="1"/>
                </group>
                <footer>
                    <button string="Send &amp; Close Ticket" 
                            name="action_send_mail" 
                            type="object" 
                            class="btn-primary" 
                            data-hotkey="q"/>
                    <button string="Discard" 
                            class="btn-secondary" 
                            special="cancel" 
                            data-hotkey="z"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>
