<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_generate_bidding_portal_url" model="ir.actions.server">
        <field name="name">Generate Portal URL</field>
        <field name="model_id" ref="model_logistic_order"/>
        <field name="binding_model_id" ref="model_logistic_order"/>
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code">
            if records:
                action = records.action_generate_portal_url()
        </field>
    </record>
</odoo>
