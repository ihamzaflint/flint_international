<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_server_action_form" model="ir.ui.view">
        <field name="model">ir.actions.server</field>
        <field name="inherit_id" ref="base.view_server_action_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page invisible="state != 'mass_edit'" name="mass_edit_line_ids" string="Fields">
                    <group>
                        <field colspan="4" name="mass_edit_line_ids" nolabel="1">
                            <tree editable="bottom">
                                <field invisible="1" name="model_id"/>
                                <field invisible="1" name="server_action_id"/>
                                <field name="sequence" widget="handle"/>
                                <field name="field_id" options="{'no_create': True, 'no_create_edit': True}"/>
                                <field name="widget_option"/>
                                <field name="apply_domain"/>
                            </tree></field>
                        <field invisible="1" name="mass_edit_apply_domain_in_lines"/>
                        <div class="text-warning" colspan="2" invisible="mass_edit_apply_domain_in_lines == False">
                            <b>
                                WARNING
                            </b>
                            : Take into account that adding a field with a domain, and not including the fields of such domain in this operation definition, will lead to an error when trying to perform it. Make sure you include them.
                        </div>
                    </group>
                </page>
                <page invisible="state != 'mass_edit'" name="mass_edit_message" string="Message">
                    <field name="mass_edit_message"/>
                </page>
            </xpath>
</field>
    </record>
</odoo>
